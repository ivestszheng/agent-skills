## 代码格式与符号规范
- **严禁转义**：在生成、补全或修改代码时，绝对禁止将代码中的尖括号 `<` 和 `>` 转换为 HTML 实体（如 `&lt;`、`&gt;` 或 `&amp;lt;`）。
- **原始输出**：必须始终输出原始的 `<` 和 `>` 字符，以确保泛型（Generics）、JSX/TSX 标签、比较运算符等代码语法的绝对正确性。
- **代码纯净**：输出的代码块必须是纯净的源代码，不要包含任何用于展示目的的 HTML 转义字符。